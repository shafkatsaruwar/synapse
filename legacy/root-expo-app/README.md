# Archived Root Expo App

This folder contains the retired root Expo app that previously lived at the repo root.

It was archived so the active product has a single clear app entrypoint:

- [`/Users/mohammed/Developer/Synapse/synapse-reset`](/Users/mohammed/Developer/Synapse/synapse-reset)

Why it was archived:

- the active iOS app and current feature work live in `synapse-reset`
- the old root app was still exposing a focus prototype on web
- leaving both app entrypoints active was causing deployment confusion

Use this archive only as historical reference. Do not point builds or deployments at it.
